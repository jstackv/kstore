# Only needed in production, when the frontend and backend are on different
# domains (e.g. separate Vercel projects). Point this at the deployed
# backend's URL, no trailing slash. Leave unset for local development -
# Vite's dev proxy (see vite.config.js) forwards /api to localhost:5000.
VITE_API_URL=https://your-backend-project.vercel.app
